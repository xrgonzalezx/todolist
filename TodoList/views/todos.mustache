<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Todo</title>
  <link rel="stylesheet" href="styles.css">
</head>

<body>
  <div class="todos">
    <form action="/" method="POST">
      <input type="text" name="newTodo" placeholder="Add a todo..." />
      <button type="submit" placeholder="Add todo">Add todo</button>
    </form>
  </div>

  <div class="tasks">
    <ul>
      {{ #todos }}
      <li>
        {{ #complete }} <del> {{ /complete }}
        {{ text }}
        {{ #complete }} </del> {{ /complete }}
        <form action="/mark-complete/{{ id }}" method="POST">
          <button type="submit" placeholder="Mark complete">Mark complete</button>
        </form>
      </li>
      {{ /todos }}
    </ul>

  </div>
</body>
</html>
